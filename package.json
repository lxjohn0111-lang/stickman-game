{
  "name": "one-way-out",
  "version": "1.0.0",
  "private": true,
  "description": "One Way Out - a first-person 3D stickman shooter for the browser",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "start": "node server.js",
    "test": "node tests/run-all.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
